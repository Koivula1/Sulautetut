import React from 'react';

function Weather() {

/*  const initWeather = []
    const [weather, setWeather] = useState(initWeather);

    fetch('https://samples.openweathermap.org/data/2.5/weather?q=London&appid=439d4b804bc8187953eb36d2a8c26a02')
    .then(response => response.json())
    .then(json => setJobs([...json]));
    */
    return (
        <p>Sääasematietoja</p>
    )
}

export default Weather;